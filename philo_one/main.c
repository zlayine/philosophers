#include <stdio.h>
#include <unistd.h>
#include <sys/time.h>

// micro to second : 1000000 => 1
int main()
{
	// int mcro_scnd = 1000000 * 5;
	// printf("start\n");
	// usleep(mcro_scnd);
	// printf("end");

	// struct timeval current_time;
	// gettimeofday(&current_time, NULL);
	// printf("seconds : %ld\nmicro seconds : %d", current_time.tv_sec, current_time.tv_usec);

	return (0);
}